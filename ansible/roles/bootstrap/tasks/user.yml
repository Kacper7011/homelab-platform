- name: Create ansible group
  ansible.builtin.group:
    name: ansible
    state: present
  
- name: Create ansible user for VM provisioning
  ansible.builtin.user:
    name: ansible
    group: ansible
    create_home: yes
    shell: /bin/bash
    state: present
