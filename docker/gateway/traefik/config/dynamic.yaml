http:
  routers:
    navidrome:
      rule: "Host(`music.kacper-nxn.pl`)"
      entrypoints: ["web"]
      service: navidrome-svc

  services:
    navidrome-svc:
      loadBalancer:
        servers:
          - url: "http://10.10.10.115:4533"